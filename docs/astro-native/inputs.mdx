---
sidebar_position: 3
---

# Inputs

```SnackPlayer dependencies=@magnetis/astro-native,react-native-svg,@magnetis/astro-native,@magnetis/astro-galaxy-tokens,@expo-google-fonts/inter

import React from 'react';
import { Text, View } from 'react-native';
import { TextInput } from '@magnetis/astro-native';


const App = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center", padding: "20px" }}>
      <TextInput onBlur={()=>{}} onFocus={()=>{}} onChangeText={()=>{}} label="hello"/>
    </View>
  );
}

export default App;
```
